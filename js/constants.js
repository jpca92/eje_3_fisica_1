export const DEFAULTS = Object.freeze({
  normalForce: 210,
  forceCase1: 42,
  forceCase2: 21,
});

export const ANIMATION = Object.freeze({
  pullDurationMs: 2400,
  thresholdDurationMs: 900,
  slideDurationMs: 2200,
  maxBottleTranslationPx: 390,
  maxBarWidthPx: 220,
});
